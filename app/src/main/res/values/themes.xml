<resources>
    <!-- Base application theme. -->
    <style name="Theme.CDS29cm" parent="Theme.Material3.Light.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/grayscale_black</item>
        <!-- Remove font padding. -->
        <item name="android:includeFontPadding">false</item>

    </style>
</resources>